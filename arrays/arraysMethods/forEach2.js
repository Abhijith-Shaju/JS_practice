// Given an array of names ["Alice", "Bob", "Charlie"], use forEach to log each name in uppercase.

const arr = ["Alice", "Bob", "Charlie"];

arr.forEach( (items) => console.log(items.toString().toUpperCase()) );